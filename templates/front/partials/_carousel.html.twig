<div id="carouselExample" class="carousel slide mb-3">
    <div class="carousel-inner">

        {% for media in trick.gallery %}
            <div class="carousel-item {% if (loop.index == 1) %}active{% endif %}">
                {% if instanceOf(media, "App\\Entity\\Image") %}
                    <img class="d-block w-100" src="{{ asset('images/trick/' ~ media.path) }}" />
                {% endif %}

                {% if instanceOf(media, "App\\Entity\\Video") %}
                    {{ media.path | raw }}
                {% endif %}
            </div>
        {% endfor %}
    </div>

    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>

    <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>

</div>