{% extends 'base.html.twig' %}

{% block title %}Ré initialiser votre mot de passe | {{ parent() }}{% endblock %}

{% block body %}
    <section class="p-3 p-lg-5">
        {{ form_start(form) }}

            <header class="mb-3">
                <h1 class="h3 mb-0 fw-bold text-uppercase">Ré initialiser votre mot de passe</h1>
            </header>

            {{ form_row(form.plainPassword) }}

            <div>
                <button class="btn btn-primary scale">Ré initialiser</button>
            </div>

        {{ form_end(form) }}
    </section>
{% endblock %}
