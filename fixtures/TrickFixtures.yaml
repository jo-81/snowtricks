App\Entity\Trick:
  trick_{1..20}:
    title (unique): '<sentence()>'
    slug (unique): '<slug()>'
    content: '<text(2000)>'
    author: '@user_<numberBetween(1, 10)>'
    category: '@category_<numberBetween(1, 10)>'
    valided: '<boolean()>'